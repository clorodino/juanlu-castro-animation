---
interface Props {
  scr: string;
  title: string;
}
const { scr, title } = Astro.props;
---

<div >
<!-- <div class="max-w-2xl overflow-hidden aspect-video rounded-xl"> -->
  <img class="w-full overflow-hidden rounded-xl object-center object-cover" src={scr} alt={title}>
</div>
